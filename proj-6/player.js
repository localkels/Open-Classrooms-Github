
export default class Player {

  constructor(xPos, yPos, hp, giftType, giftStrength) {
    this.xPos = xPos; // positions (x and y)
    this.yPos = yPos;
    this.hp = hp; // happy points
    this.giftType = giftType; // better gifts give more hp
    this.giftStrength = giftStrength; // better gifts give more hp
    this.move = function () {

    };
    this.give = function () {

    };
  }
  
};
